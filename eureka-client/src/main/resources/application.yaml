spring:
  application:
    name: admin-service

server:
  port: 8080

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serverviceUrl:
      defaultZone: http://localhost:8761/eureka/
    instance:
      hostname: localhost